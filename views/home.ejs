<%- include("partials/header"); -%>

<h1> Hello </h1>
<p><%= para1 %></p>

<% posts.forEach(function(post){%>
	<h1><%= post.vTitle %></h1>
	<p><%= (post.vText).substring(0, 50) + "..." %> <a href = "/posts/
	<%=post._id%>">Read more</a></p>
<% }); %>

<% posts.forEach(function(post){%>
<% console.log(post.vTitle); %>
<% }); %>

<%- include("partials/footer"); -%>